<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE glassfish-web-app PUBLIC 
"-//GlassFish.org//DTD GlassFish Application Server 3.1 Servlet 3.0//EN"
 "http://glassfish.org/dtds/glassfish-web-app_3_0-1.dtd">
<glassfish-web-app error-url="">
    <context-root>jerseyEasy</context-root>
    <parameter-encoding default-charset="UTF-8" />
    <!-- Here is the problem, you should have your path of "dir" to the 
    containing folder you wish to share, so in "from" you set the name of the 
    folder and that is all, you should be able to access to the contents of
    the folder -->
    <!--
    <property name="alternatedocroot_1" value="from=/userPic/* dir=${resourceRoot}" />
    <property name="alternatedocroot_2" value="from=/apk/* dir=${resourceRoot}" />
    <property name="alternatedocroot_3" value="from=/terminalLog/* dir=${resourceRoot}" />
    <property name="alternatedocroot_4" value="from=/syslog/* dir=${resourceRoot}" />
    <property name="alternatedocroot_5" value="from=/errorlog/* dir=${resourceRoot}" />
    <property name="alternatedocroot_2" value="from=/docroot/* dir=${docpath}" />
    <property name="alternatedocroot_1" value="from=/apk/* dir=/var/www/html/upload/" />
    <property name="alternatedocroot_2" value="from=/docroot/* dir=/usr/local/glassfish3/glassfish/domains/domain1/" />
    <property name="alternatedocroot_1" value="from=/hello/* dir=D:\usr\local\ET\" />
     in this case, the contaning folder is D:\ and the folder to share is 
    "media" so the requests to "localhost:8080/MyApplication/media/" should 
    redirect to D:\media\ -->
</glassfish-web-app>